name: Deploy Flutter Web Client to Firebase Hosting

on:
  push:
    branches:
      - main
    paths:
      - 'client/**'
      - '.github/workflows/deploy-client-web.yml'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Create Empty Env Asset File
        run: |
          cd client
          touch .env

      - name: Install Client Dependencies
        run: |
          cd client
          flutter pub get

      - name: Run Client Unit Tests
        run: |
          cd client
          flutter test

      - name: Build Flutter Web Release
        env:
          RAW_API_URL: ${{ secrets.API_BASE_URL }}
          RAW_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
        run: |
          cd client
          API_URL=$(echo "$RAW_API_URL" | tr -d '\r\n')
          CLIENT_ID=$(echo "$RAW_CLIENT_ID" | tr -d '\r\n')
          flutter build web --release --dart-define=API_BASE_URL="$API_URL" --dart-define=GOOGLE_CLIENT_ID="$CLIENT_ID"

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.GCP_SA_KEY }}'
          channelId: live
          projectId: jobcruiser
          entryPoint: client
